export const metadata = {
  title: 'Dashboard - ResumeInsight',
  description: 'Your personal resume analysis dashboard. View your history, stats, and start new analyses.',
};

export default function DashboardLayout({ children }) {
  return children;
}
